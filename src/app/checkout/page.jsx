import CheckoutPage from "./Checkout";

export const metadata = {
  title: "Checkout",
  discription: "Write Your data and pay your total price",
};

const checkout = () => {
  return <CheckoutPage />;
};

export default checkout;
